---
import type { BookMeta } from "@/libs/book";

interface Props extends BookMeta {}

const { slug, title } = Astro.props;
---

<a href={`/articles/${slug}`} class="flex items-center gap-2">
  <h3 class="text-xl font-bold">{title}</h3>
</a>
